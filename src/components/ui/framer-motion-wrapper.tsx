"use client";

// Import directly from framer-motion
import { 
  motion,
  AnimatePresence,
  useAnimation,
  useMotionValue,
  useTransform,
  useScroll,
  useSpring
} from "framer-motion";

// Re-export the components
export { 
  motion,
  AnimatePresence,
  useAnimation,
  useMotionValue,
  useTransform,
  useScroll,
  useSpring
};
